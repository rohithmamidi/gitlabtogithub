name: caller Metadata Extraction

on:
    push:
      branches:
        - feature/metadataworkflow

jobs:
  call-reusable-workflow:
    uses: rohithmamidi/gitlabtogithub/.github/workflows/run-metadata.yml@feature/metadataworkflow
    with:
      github-org: rohithmamidi
    secrets:
      GITLABTOKEN: ${{ secrets.GITLABTOKEN }}
      GITHUBTOKEN: ${{ secrets.GITHUBTOKEN }}